# Voice Server (WebSocket)
# Overrides the default Railway production URL if set
# NEXT_PUBLIC_VOICE_WS_URL=wss://nevermissacall-production-23c7.up.railway.app/ws/web

# Supabase (server-side only — use service role key, NOT anon key)
# SUPABASE_URL=https://<your-project>.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>
